## Summary

<!-- What changed and why. Link related issues. -->

## Validation

<!-- Paste the result of `validation-md run` (or `/validation run`). -->

```text

```

- [ ] `npm run check` passes
- [ ] `VALIDATION.md` was updated if this change adds or removes a required artifact
- [ ] Docs (`README.md`, `examples/`, `skills/`) updated if user-facing behavior changed

## Notes for reviewers

<!-- Anything that is hard to see from the diff: design tradeoffs, follow-ups, known gaps. -->
